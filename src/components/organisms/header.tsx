import SettingsMenu from './settings_menu';
import './header.scss';

function Header() {
    return (
        <header className='header'>
            <SettingsMenu />
        </header>
    )
};

export default Header;